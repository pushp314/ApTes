{
  "name": "sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "Security and software-quality testing platform with deterministic analysis engines",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@sentinel/shared && npm run build --workspace=@sentinel/codesentinel && npm run build --workspace=@sentinel/web && npm run build --workspace=@sentinel/mcp && npm run build --workspace=@sentinel/platform",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint 'packages/*/src/**/*.ts'",
    "lint:fix": "eslint 'packages/*/src/**/*.ts' --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
