/**
 * @sentinel/shared
 *
 * Shared contracts, interfaces, and utilities for the Sentinel platform.
 *
 * This package will contain:
 * - Finding interface (shared across all engines)
 * - EngineRule interface
 * - Severity/confidence types
 * - Common configuration conventions
 *
 * These will be implemented in Phase 1 (Shared Contracts).
 */

/** Sentinel platform version */
export const VERSION = '0.1.0';
