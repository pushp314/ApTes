{
  "name": "@sentinel/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contracts, interfaces, and utilities for the Sentinel platform",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/pino": "^7.0.4",
    "@types/pino-pretty": "^4.7.5"
  }
}
